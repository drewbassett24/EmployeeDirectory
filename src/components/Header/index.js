import React from "react";
import "./style.css";

const Header = () => (
  <div className="header">
    <h1>Employee Directory</h1>
    <p>
      Search employees by name. Click the column headings
      to sort.
    </p>
  </div>
);

export default Header;